type Query {
  shows(titleFilter: String): [Show]
  movies: [Movie]
}

type Show {
  title: String
  releaseYear: Int
}

type Movie {
  id: ID
  title: String
  director: Director
}

type Director {
  id: ID
  name: String
}
